# license-plate-detector
A Python project that uses YOLO and OpenCV to detect car license plates in images.  The program draws a thick red bounding box around detected plates and shows an enlarged version of the plate inside the image.
